// Entry point for the dots-vue-config-validator package
// Export the Zod schema so consumer projects can reuse it.
import { DotsConfig, GenericConfig } from "./schema.js";

export { DotsConfig, GenericConfig };
